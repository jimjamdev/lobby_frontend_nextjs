import { TPage } from '~types/pages/Page.types';

// eslint-disable-next-line react/function-component-definition
const GamePage: TPage = () => (
  <div>
    <h1>Game</h1>
  </div>
);

export default GamePage;
